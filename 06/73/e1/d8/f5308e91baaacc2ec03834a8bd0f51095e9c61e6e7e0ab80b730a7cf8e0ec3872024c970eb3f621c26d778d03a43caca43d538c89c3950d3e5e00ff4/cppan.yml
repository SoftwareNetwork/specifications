source:
    git:
        url: https://github.com/madler/zlib
        tag: v{v}

version: 1.2.7

name: madler.zlib

type: lib

files:
    -   "[^/]*\\.c"
    -   "[^/]*\\.h"

options:
    shared:
        definitions:
            private:
                -   ZLIB_DLL
